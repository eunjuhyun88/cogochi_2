{
  "name": "cogochi",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || echo ''",
    "check:architecture": "node scripts/check-aimon-architecture.mjs",
    "check:context": "node scripts/check-context-docs.mjs",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json && npm run check:architecture && npm run check:context"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.0",
    "@sveltejs/kit": "^2.50.2",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.51.0",
    "svelte-check": "^4.3.6",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
